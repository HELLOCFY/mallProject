//home组件小仓库
//state为数据
const state={
};
//mutations为修改state的唯一手段
const mutations={
};
//actions处理action，可以书写业务逻辑和书写异步
const actions={

};
//getters可以理解为计算属性，用于简化仓库数据，让组件获取仓库数据更加方便
const getters = {
};


export default {
    state,
    mutations,
    actions,
    getters,
}